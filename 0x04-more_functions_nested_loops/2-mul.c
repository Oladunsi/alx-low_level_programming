#include "main.h"

/**
* mul - takes two parameter and multiply them
* @a: a number
* @b: a number
*
* Return: the product of a and b
*/

int mul(int a, int b)
{
	int value = a * b;

return (value);
}
