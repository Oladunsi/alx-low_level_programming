#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void _puts(char *str)
{
	int len, n;
	
	len = _strlen(str);

	for (n = 0; n <= len; n++)
	{
		_putchar(len);
	}
	_putchar('\n');
}
